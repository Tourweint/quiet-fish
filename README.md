# 安静养鱼 - 自习神器

一个有趣的自律工具：保持安静，鱼才会留下来陪你学习 🎓

## 效果

- 🔇 **安静** → 鱼儿游来陪你
- 🔊 **太吵** → 鱼都跑光了

## 游戏特性

- 🐟 **稀有鱼系统**：普通 → 稀有 → 史诗 → 传说 → 神话
- ✨ **发光效果**：稀有以上鱼有发光特效
- 📊 **统计系统**：积分、等级、安静时长统计
- 🍅 **番茄钟**：专注计时功能
- 🏆 **成就系统**：10+ 成就等你解锁
- 🎮 **操作**：空格键开启番茄钟，S 截图

## 安装

```bash
# 克隆后
cd quiet_fish

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# 安装依赖
pip install -r requirements.txt
```

## 运行

```bash
python main.py
```

## 依赖

- pygame >= 2.0
- PyAudio

## 项目结构

```
quiet_fish/
├── main.py           # 主程序入口
├── config.py         # 配置（稀有度、成就、等级等）
├── models/
│   ├── audio.py      # 音频监控
│   ├── fish.py       # 鱼类逻辑
│   ├── bubble.py     # 气泡粒子
│   └── stats.py      # 统计数据与成就
├── ui/
│   ├── font_manager.py  # 字体管理
│   └── panel.py      # UI面板组件
├── data/             # 数据存储（成就、统计JSON）
└── requirements.txt
```
